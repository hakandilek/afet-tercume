import { HeaderLabel } from '../../header/header.label';
import { LanguageLabel } from '../../language/language.label';
import { SettingsLabel } from '../../settings/settings.label';
import { TermLabel } from '../../terms/terms.label';

export interface UiLabels {
  languagePage: LanguageLabel;
  termsPage: TermLabel;
  headerSection: HeaderLabel;
  settingsPage: SettingsLabel;
}
